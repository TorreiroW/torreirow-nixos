session: Witte Velden

root: /home/wtoorren/data/git/torreirow/temp/stwv/cohub-test

stop:

windows:
  - name: sync
    layout: main-horizontal
    commands:
      - watch -n 10 git-sync -n
    panes:
      - type: horizontal
        commands:
          - hugo server --ignoreCache --forceSyncStatic --cleanDestinationDir --disableFastRender -e private -p 1312
  - name: infra
    layout: main-horizontal
    commands:
      - xdg-open http://localhost:1312
      - vim -c NERDTree
  - name: cmd
    layout: main-horizontal
